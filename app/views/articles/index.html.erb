<!-- index.html.erb -->
<% @articles.each do |article| %>
  <h2 class="mt-4 border m-0 p-3"><%= link_to article.title, article_path(article) %></h2>
  <p class="border m-0 p-3"><%= article.content %></p>
<% end %>

